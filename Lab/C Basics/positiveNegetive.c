#include <stdio.h>
#include <string.h>

int main()
{
    int n;
    scanf("%d", &n);
    if (n >= 0)
    {
        printf("Positive");
    }
    else
    {
        printf("Negetive");
    }

    return 1;
}