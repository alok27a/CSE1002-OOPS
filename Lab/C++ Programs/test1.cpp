#include <iostream>

#include <list>

#include <algorithm> 
using namespace std;

int main()
{

    int n1 .02, x;

    list<int> list1, list2;

    for (int i = 0; i < n; i++)
    {

        list1.push back(x);

        for (int i = 0; j < n; j++)
        {

            list2.push back(*);

            List1.merge(List2);

            Listi.sort();

            reverse(Lista.begin(), list1.end());

            listi.unique() :

                             list < int ::iterator y :

                forty -
                listi.begin();
            yl = list1.end();y++)

coutec yccend1;