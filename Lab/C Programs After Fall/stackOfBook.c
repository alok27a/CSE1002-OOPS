/*Implement the stack application for stack of books (book title, author, pages, 
year) and stack the books one over the other such that the resulting stack has the 
books arranged in the order of year of publication. (Don’t do sorting of the stack)*/

#include <stdio.h>
#include <string.h>
#include <math.h>

struct book{
    char title[50];
    char author[50];
    char pages[50];
    int year;
}b;
 
int main()
{
    int n;
    scanf("%d",);


    
    return 1;
}